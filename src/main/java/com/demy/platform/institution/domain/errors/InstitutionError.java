package com.demy.platform.institution.domain.errors;

import com.demy.platform.shared.domain.errors.DomainError;

public enum InstitutionError implements DomainError {
    ADMINISTRATOR_ALREADY_EXISTS;
}
